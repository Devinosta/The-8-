<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The 8% Lending</title>
    <style>
        :root {
            --primary: #1a1a1a; /* Black from logo */
            --accent: #ffc107;  /* Gold from logo */
            --bg: #fdfdfd;
            --white: #ffffff;
        }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; background: var(--bg); color: #333; }
        header { background: var(--primary); color: white; padding: 2rem 1rem; text-align: center; border-bottom: 5px solid var(--accent); }
        
        .logo-image { 
            max-width: 280px; 
            height: auto; 
            margin-bottom: 10px;
        }

        .container { max-width: 900px; margin: auto; padding: 20px; }
        .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
        @media (max-width: 600px) { .grid { grid-template-columns: 1fr; } }

        .card { background: var(--white); padding: 25px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.08); border-top: 3px solid var(--accent); }
        
        input { width: 100%; padding: 12px; margin: 10px 0; border: 1px solid #ddd; border-radius: 6px; box-sizing: border-box; font-size: 1rem; }
        .btn { background: var(--primary); color: var(--accent); border: 2px solid var(--accent); padding: 15px; cursor: pointer; width: 100%; font-size: 1.1rem; border-radius: 6px; font-weight: bold; transition: 0.3s; }
        .btn:hover { background: var(--accent); color: var(--primary); }
        
        #result { margin-top: 15px; padding: 15px; background: #fff9e6; border-radius: 5px; color: #856404; font-weight: bold; text-align: center; border: 1px solid var(--accent); }
        
        .requirements { list-style: none; padding: 0; }
        .requirements li::before { content: "● "; color: var(--accent); font-weight: bold; padding-right: 8px; }
        
        footer { text-align: center; padding: 2rem; color: #7f8c8d; font-size: 0.8rem; }
    </style>
</head>
<body>

<header>
    <img src="The 8% Lending 2.png" alt="The 8% Lending Logo" class="logo-image">
</header>

<div class="container">
    <div class="grid">
        <div class="card">
            <h3>Loan Calculator</h3>
            <p>Calculate your monthly repayment at our standard 8% rate.</p>
            <label>Amount to Borrow (₱)</label>
            <input type="number" id="amount" placeholder="Enter amount">
            <button class="btn" onclick="calculate()">Calculate Now</button>
            <div id="result" style="display:none;"></div>
        </div>

        <div class="card">
            <h3>Requirements</h3>
            <ul class="requirements">
                <li>Valid Government ID</li>
                <li>Latest Payslip / Proof of Income</li>
                <li>Proof of Billing</li>
                <li>Active Mobile Number</li>
            </ul>
        </div>
    </div>

    <div class="card" style="margin-top: 20px; text-align: center; background: var(--primary); color: white;">
        <h3 style="color: var(--accent);">Get Started Today</h3>
        <p>Message us directly to start your application.</p>
        <p>📍 Location: Iloilo City</p>
        
        <div style="display: flex; gap: 10px; justify-content: center; flex-wrap: wrap; margin-top: 15px;">
            <button class="btn" style="width: auto; padding: 12px 30px;" onclick="window.location.href='mailto:your-email@gmail.com'">Email Us</button>
            <button class="btn" style="width: auto; padding: 12px 30px; background: var(--accent); color: var(--primary);" onclick="window.location.href='https://m.me/yourpage'">Messenger</button>
        </div>
    </div>
</div>

<footer>
    &copy; 2026 The 8% Lending. All rights reserved.
</footer>

<script>
    function calculate() {
        const amount = document.getElementById('amount').value;
        const resultDiv = document.getElementById('result');
        if(amount > 0) {
            const interest = amount * 0.08;
            const total = parseFloat(amount) + interest;
            resultDiv.style.display = "block";
            resultDiv.innerHTML = `Interest: ₱${interest.toLocaleString()}<br>Total Due: ₱${total.toLocaleString()}`;
        }
    }
</script>

</body>
</html>
